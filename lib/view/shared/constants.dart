import 'package:flutter/material.dart';

mixin ColorPalette {
  final Color backgroundColor = const Color.fromARGB(255, 252, 203, 139);
}

mixin Paddings {
  final EdgeInsets bodyPadding =
      const EdgeInsets.symmetric(vertical: 40, horizontal: 20);
}
